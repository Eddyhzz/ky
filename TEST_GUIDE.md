# 测试指南

## 当前配置

- **AI 模型**：DeepSeek Chat
- **API Key**：由服务端环境变量提供
- **配置方式**：`DEEPSEEK_API_KEY`，不使用 `VITE_` 前缀

## 测试步骤

### 1. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:5173

### 2. 测试功能

#### 测试案例 1：动作片
**故事概念**：
```
一个退休的特工发现自己的女儿被绑架了。绑匪要求巨额赎金。
他必须利用自己的特殊技能追踪绑匪，但在调查过程中发现，
这场绑架背后涉及他曾经参与的一个秘密行动。
```

**类型**：动作

**预期结果**：生成 15 个符合动作片节奏的故事节拍

#### 测试案例 2：爱情喜剧
**故事概念**：
```
李明是一个直男癌程序员，在相亲 app 上屡战屡败。他的闺蜜
建议他参加一个脱单训练营。在训练营里，他遇到了教练张婷，
一个看似完美但其实也有感情创伤的女生。两人在互相指导对方
脱单的过程中，逐渐产生好感。
```

**类型**：爱情

**预期结果**：生成 15 个符合爱情喜剧节奏的故事节拍

#### 测试案例 3：科幻惊悚
**故事概念**：
```
2045年，记忆可以被数字化存储。侦探陈宇接到一个案件：
一位富豪的数字记忆被盗。陈宇进入受害者的记忆世界调查，
却发现记忆中存在无法解释的矛盾。他开始怀疑：这些记忆
是真实的吗？
```

**类型**：科幻

**预期结果**：生成 15 个符合科幻惊悚节奏的故事节拍

### 3. 功能检查清单

- [ ] 输入故事概念（必填）
- [ ] 选择故事类型
- [ ] 点击"生成故事节拍"按钮
- [ ] 显示"生成中..."加载状态
- [ ] 成功生成 15 个节拍
- [ ] 每个节拍包含：序号、标题、描述、时间占比
- [ ] 节拍卡片 hover 时边框高亮
- [ ] 可以点击"重新生成"
- [ ] 错误处理正常（网络错误、API 错误等）

### 4. 部署测试

```bash
# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

访问 http://localhost:4173 测试生产环境

## 已知配置

- API Key 通过服务端环境变量 `DEEPSEEK_API_KEY` 配置
- DeepSeek API 端点：https://api.deepseek.com/v1/chat/completions
- 模型：deepseek-chat
- Temperature: 0.7
- Max Tokens: 2000

## 故障排查

### 如果生成失败

1. 检查 `.env` 文件是否存在
2. 检查 API Key 是否正确
3. 检查网络连接
4. 查看浏览器控制台错误信息

### 如果 API Key 未生效

Vite 的环境变量需要重启开发服务器才能生效：
```bash
# 停止服务器 (Ctrl+C)
# 重新启动
npm run dev
```

## 演示录屏要点

1. **展示输入流程**
   - 输入完整的故事概念
   - 选择合适的类型
   - 点击生成按钮

2. **展示生成过程**
   - 加载状态
   - 等待 5-10 秒

3. **展示结果**
   - 滚动查看所有 15 个节拍
   - 展示节拍的详细内容
   - 演示"重新生成"功能

4. **说明技术实现**
   - DeepSeek API 集成
   - 环境变量配置
   - 前后端分离架构

# 故事节拍规划器 - 产品设计文档

## 一、产品定位

### 目标用户
- **主要用户**：网文作者、短视频编剧、自媒体内容创作者
- **次要用户**：想要系统学习叙事结构的业余创作者
- **用户痛点**：
  - 有好的故事点子，但不知道如何组织成完整的叙事结构
  - 缺乏系统的编剧训练，不了解经典的叙事模型
  - 需要快速产出结构合理的故事大纲

### 核心价值主张
将零散的故事概念转化为清晰、可执行的叙事节拍结构，让创作者专注于故事细节而非结构框架。

### 为什么选择这个方向
1. **明确的痛点**：大量内容创作者面临"有想法但写不出来"的困境
2. **AI 的价值清晰**：结构化输出是 LLM 的强项，不是简单的文本生成
3. **范围可控**：聚焦在故事规划阶段，不涉及完整创作流程
4. **可验证性强**：生成的节拍结构是否合理，用户能立即判断

## 二、功能设计

### 核心功能流程
1. 用户输入故事概念（100-500 字）
2. 选择故事类型（剧情/动作/喜剧/惊悚/爱情/科幻）
3. 输入 Anthropic API Key（本地存储，不上传）
4. AI 生成 15 个基于"Save the Cat"模型的故事节拍
5. 展示结构化的节拍列表，包含标题、描述、时间占比

### 为什么选择"Save the Cat"结构
- 业界广泛认可的叙事模型
- 15 个节拍划分清晰，适合 AI 生成
- 适用于多种类型的故事（电影、短剧、小说）

### AI 参与方式
- **输入**：故事概念 + 类型标签
- **处理**：使用 Claude 3.5 Sonnet，prompt 引导生成结构化 JSON
- **输出**：15 个节拍，每个包含中英文标题、具体描述、时间占比
- **特点**：不是简单的模板填充，AI 会根据具体概念生成针对性的场景描述

## 三、技术实现

### 技术栈选择理由
- **React 19 + TypeScript**：类型安全，开发效率高
- **Vite**：快速的开发体验
- **Tailwind CSS 4**：快速实现设计系统
- **Claude API**：高质量的结构化输出能力
- **Vercel**：零配置部署

### API 集成方式
```typescript
// 直接在浏览器调用 Anthropic SDK
// dangerouslyAllowBrowser: true
// 用户自己提供 API Key，不经过后端
```

### 为什么选择客户端直连
1. **简化架构**：3 小时内可完成 MVP
2. **隐私友好**：API Key 不离开用户浏览器
3. **成本为零**：不需要维护后端服务器
4. **快速验证**：专注产品核心价值

## 四、设计系统

### 色彩方案
- **背景层次**：5 层深色表面（#05070C → #1E2636）
- **强调色**：青色 #38BDF8（高饱和度，视觉焦点）
- **文本色**：白色标题 + 灰色正文（层次清晰）

### 设计原则
- **专注氛围**：深色调减少视觉干扰
- **信息层次**：用表面层次和色彩区分内容重要性
- **可操作性**：大按钮、清晰的焦点状态

## 五、完成边界

### ✅ 已实现
- 完整的用户输入界面
- Claude API 集成（15 节拍生成）
- 节拍可视化展示
- 错误处理和加载状态
- 响应式设计
- 重新生成功能

### 🚧 未实现（按优先级）
1. **导出功能**：导出为 Markdown/PDF，方便创作者后续使用
2. **节拍编辑**：允许用户微调 AI 生成的内容
3. **历史记录**：保存生成过的结构，便于对比
4. **示例故事**：提供经典电影的节拍分析作为参考

### 为什么这样划分优先级
- **已实现**：核心用户路径完整可用，能够真实验证产品价值
- **未实现**：增强型功能，不影响核心验证

## 六、使用说明

### 用户操作步骤
1. 访问产品页面
2. 输入 Anthropic API Key（从 console.anthropic.com 获取）
3. 在文本框中描述故事概念
4. 选择故事类型
5. 点击"生成故事节拍"
6. 等待 5-10 秒，查看生成的 15 个节拍
7. 可点击"重新生成"尝试不同的结果

### 示例输入
```
故事概念：一个退休的特工发现自己的女儿被绑架了。他必须
利用自己的特殊技能追踪绑匪，但发现绑架背后涉及他曾经参与
的秘密行动，他必须面对自己的过去。

类型：动作
```

## 七、AI 在开发中的作用

### AI 如何参与开发
1. **快速搭建**：使用 AI 生成 Vite + React + Tailwind 的基础结构
2. **组件实现**：AI 帮助实现 UI 组件，保持设计一致性
3. **API 集成**：AI 提供 Anthropic SDK 集成代码
4. **调试**：AI 协助解决 TypeScript 类型错误、构建问题

### AI 的局限性
- **设计决策**：产品定位、功能范围、用户体验由人决定
- **代码审查**：需要人工检查 API Key 安全性、错误处理逻辑
- **调试复杂问题**：Tailwind v4 配置问题需要人工介入

## 八、实际投入时间

- **产品规划**：30 分钟（定位、功能范围、技术选型）
- **开发实现**：2 小时（UI + API 集成 + 调试）
- **测试验证**：20 分钟（本地测试、构建验证）
- **文档准备**：10 分钟（README + 产品文档）

**总计：约 3 小时**

## 九、下一步优先级

如果继续开发，第一优先级是：

### 导出功能（30 分钟可完成）
```typescript
// 将节拍导出为 Markdown 格式
function exportBeats(beats: Beat[], concept: string) {
  const markdown = `# 故事节拍\n\n概念：${concept}\n\n` +
    beats.map((beat, i) => 
      `## ${i+1}. ${beat.title}\n\n${beat.description}\n\n时间：${beat.timing}\n\n`
    ).join('')
  
  // 下载文件
  const blob = new Blob([markdown], { type: 'text/markdown' })
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = 'story-beats.md'
  a.click()
}
```

**理由**：导出是用户最直接的需求，生成的结构需要保存下来继续创作。

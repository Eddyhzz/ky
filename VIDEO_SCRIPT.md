# 录屏汇报脚本（5 分钟）

## 开场（30 秒）

大家好，我是候选人 [姓名]。这次实操我做了一个 **AI 故事节拍规划器**。

这个产品解决的问题是：很多内容创作者有好的故事点子，但不知道如何组织成完整的叙事结构。我的解决方案是：用 AI 将故事概念转化为清晰的故事节拍框架。

接下来我会演示产品功能，说明关键决策，以及 AI 如何参与开发。

## 产品演示（2 分钟）

### 打开页面
首先看到的是产品首页。深色主题营造专注的创作氛围，使用层次化的表面色和高饱和度的青色作为强调色。

### 输入流程
1. **API Key**：用户首先输入 Anthropic API Key。这里我要特别说明：API Key 仅在浏览器本地使用，不会上传到任何服务器，保护用户隐私。

2. **故事概念**：我输入一个示例：
   ```
   一个退休的特工发现女儿被绑架，必须利用特殊技能追踪绑匪，
   但发现绑架背后涉及他曾经参与的秘密行动。
   ```

3. **选择类型**：选择"动作"

### 生成节拍
点击"生成故事节拍"，等待 5-10 秒。

### 查看结果
AI 生成了 15 个基于 Save the Cat 结构的故事节拍：
- 每个节拍包含中英文标题、具体描述、时间占比
- 描述不是模板化的，而是针对这个具体故事生成的
- 例如这里的"开场"描述的是"约翰过着平静的退休生活"，针对我们输入的故事

可以点击"重新生成"尝试不同的版本。

## 关键决策（1.5 分钟）

### 为什么做这个？

我选择故事节拍规划器有三个原因：

1. **明确的用户痛点**：很多网文作者、短视频编剧面临"有想法但写不出来"的困境，缺乏系统的叙事训练。

2. **AI 的价值清晰**：这不是简单的文本生成或续写，而是结构化输出。AI 能够理解故事概念，生成符合经典叙事模型的框架。

3. **范围可控**：聚焦在故事规划阶段，不涉及完整创作流程。在 3 小时内能完成核心用户路径。

### 为什么选择这个技术方案？

- **客户端直连 Claude API**：简化架构，无需后端服务器。API Key 不离开浏览器，隐私友好。
- **React + TypeScript + Tailwind**：类型安全，开发效率高，快速实现设计系统。
- **Vercel 部署**：零配置，自动 CDN，适合快速验证。

### 为什么选择 Save the Cat 结构？

- 业界广泛认可的叙事模型
- 15 个节拍划分清晰，适合 AI 生成
- 适用于电影、短剧、小说等多种媒介

## AI 参与开发（1 分钟）

AI 在这个项目中发挥了重要作用：

1. **快速搭建**：使用 Claude 生成 Vite + React + TypeScript 项目结构，配置 Tailwind CSS。

2. **组件实现**：AI 帮助实现 UI 组件，包括输入表单、节拍卡片，保持设计一致性。

3. **API 集成**：AI 提供 Anthropic SDK 的集成代码，处理异步请求和错误状态。

4. **调试支持**：解决 TypeScript 类型错误、Tailwind v4 的 PostCSS 配置问题。

但 AI 也有局限性：
- **产品决策**：用户定位、功能范围、体验设计是我自己决定的
- **安全审查**：API Key 处理、错误边界等需要人工检查
- **复杂问题**：某些配置问题需要理解底层原理才能解决

## 完成边界（30 秒）

### 已完成（可正常使用）
- 完整的用户输入和生成流程
- Claude API 集成，15 节拍生成
- 错误处理和加载状态
- 响应式设计
- 可部署到生产环境

### 未完成（下一步优先级）
如果继续开发，第一优先级是**导出功能**：
- 将节拍导出为 Markdown/PDF
- 便于创作者保存和后续使用
- 预计 30 分钟可完成

其次是节拍编辑和历史记录功能。

## 时间投入（30 秒）

实际投入约 **3 小时**：
- 产品规划：30 分钟（用户研究、功能定义、技术选型）
- 开发实现：2 小时（搭建、UI、API 集成、调试）
- 测试验证：20 分钟
- 文档准备：10 分钟

## 结尾（10 秒）

这就是我的故事节拍规划器。代码已推送到 Git 仓库，部署在 Vercel 上可以访问。感谢观看。

---

## 录屏技巧提示

### 准备工作
- 关闭无关浏览器标签页
- 清理桌面和通知
- 准备好测试用的 API Key
- 提前写好示例故事概念（可以直接复制粘贴）

### 录制建议
- 使用 QuickTime Player（Mac）或 OBS Studio（跨平台）
- 分辨率：1920x1080 或 1280x720
- 语速适中，清晰表达
- 可以使用画中画显示自己（可选）

### 时间分配参考
- 开场：30 秒
- 产品演示：2 分钟（最重要）
- 关键决策：1.5 分钟
- AI 参与：1 分钟
- 完成边界和时间：1 分钟

### 如果超时
优先保留：
1. 产品演示（核心功能）
2. 关键决策（为什么这样做）
3. AI 参与（岗位相关）

可以压缩：
- 开场白
- 完成边界（可以口头一笔带过）
